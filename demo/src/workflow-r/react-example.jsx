import React from 'react';
import PropTypes from 'prop-types';

import './s.scss';

const propTypes = {};

const defaultProps = {};

function s({}) {
  return (
    <div className="s">
      hi
    </div>
  );
}

s.propTypes = propTypes;
s.defaultProps = defaultProps;
export default s;
